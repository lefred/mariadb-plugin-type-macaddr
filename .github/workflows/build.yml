name: Build and package TYPE_MACADDR plugin

on:
  push:
    branches:
      - main
    tags:
      - "v*"

  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    uses: lefred/mariadb-plugin-ci/.github/workflows/build-plugin.yml@v1
    with:
      plugin_name: type-macaddr
      plugin_directory: type_macaddr
      build_target: type_macaddr
      plugin_library: type_macaddr.so
      cmake_options: -DPLUGIN_DATE_EXTRA=DYNAMIC
      test_suite: type_macaddr
      run_tests: ${{ startsWith(github.ref, 'refs/tags/v') }}
